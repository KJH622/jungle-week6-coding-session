#include "executor.h"

int execute_sql_statement(const SqlStatement *statement)
{
    /*
     * 실제 INSERT / SELECT 실행은 다음 단계에서 구현한다.
     * 지금은 main 의 전체 처리 흐름을 안정적으로 연결하는 것이 목표다.
     */
    (void)statement;
    return 1;
}
